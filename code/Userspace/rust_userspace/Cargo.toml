[package]
name = "rust_userspace"
version = "0.1.0"
edition = "2021"

[dependencies]

# telling the rust to compile this project as a library with no metadata 
[lib]
name = "rust_userspace"
crate-type = ["staticlib"]
